# 安全政策

本项目是**完全离线的本机翻译工具**：模型权重在你自己的磁盘上，推理在你自己的 NPU / CPU 上，
译文缓存也在本机。这个前提决定了它的安全模型 —— 也决定了什么算漏洞、什么不算。

---

## 支持版本

| 版本 | 是否修安全问题 |
|---|---|
| `0.1.x`（当前，`main` 分支） | ✅ |
| `main` 分支上的未发布 commit | ✅（按 commit 修，不发独立版本号） |
| 更早的 / 未发布的分支 | ❌ 不维护 |

说明：

- 项目还在 `0.1.x`，**没有旧维护线**（就一条线）。等出现 `0.2.x` 之后，这里会补上"上一个 minor 再跟 N 个月"的约定。
- 运行时的**依赖版本组合是锁定的**（`openvino` / `openvino-genai` / `openvino-tokenizers`，见 `requirements.txt` 与 `pyproject.toml`）。
  安全修复如果需要升 OpenVINO，会作为一次常规提交带上 —— 那是**行为可能变化**的改动，
  不会悄悄发在你背后。
- 平台支持矩阵以 `README.md` 为准：**只有 Windows 11 x86_64（NPU 与 CPU）是实测过的**，
  Linux / ARM 属于"能装上、代码路径能跑通"，**未做性能与安全验证**。在非实测平台上报的问题我们会看，
  但修复优先级低于实测平台。

---

## 怎么报告漏洞

🔴 **不要在公开 issue、讨论区、PR 里描述安全漏洞。**

用 GitHub 的**私密漏洞报告**（Private vulnerability reporting）：

> <https://github.com/Purter88/intel-npuTranslator/security/advisories/new>

它走 Security Advisories，只有维护者看得到，可以先私下确认、再一起决定公开时间。
仓库的 issue 表单里也做了引导（`.github/ISSUE_TEMPLATE/config.yml`）。

如果那条链接不可用（比如组织级设置把它关了），退而求其次的做法：**开一个不含任何细节的 issue 说"我有安全问题要报，请开一个私信渠道"** ——
把细节留到私密渠道，别贴在公开页面上。

### 报告里请带上

- 版本号或 commit（`python -c "import npu_translator; print(npu_translator.__version__)"`）
- 操作系统与 Python 版本
- 复现步骤（能一段脚本跑出来最好）
- 你观察到的**实际行为** vs **期望行为**
- 相关输出 —— **请先脱敏**：删掉绝对路径、token、证书与私钥内容、你自己的文本

🔴 **别把私钥贴进来**：自签私钥一旦出现在任何公开页面（issue、gist、日志、截图）就是永久泄漏，
删掉 issue 也救不回来 —— 已经 clone / 已经看过的人手里还有一份。

### 响应时间（目标，不是承诺）

| 环节 | 目标 |
|---|---|
| 确认收到 | **7 天内** |
| 初步评估（能不能复现、影响面多大） | **14 天内** |
| 修复 / 缓解方案 | 视严重度；高危会优先于新功能 |

这是一个小项目，**超时是可能发生的**。超过 14 天没回音，可以在原 Advisory 里 ping 一次 —— 更可能是漏看了，不是不管。

我们会在修复版本的 release notes 里致谢报告者（除非你明确要求匿名）。

---

## 本项目的威胁模型（先看这个，再判断你手上的是不是漏洞）

### 信任边界是"用户自己的机器"，不是"服务端防外敌"

`nputweb` / `nputserve` 是**单人本机服务**：你起它、你自己用，进程跑在你的登录会话里。
它不是给不可信网络设计的多租户服务。所以：

- **能登录这台机器 / 能物理接触这台机器的攻击者，拿到的是你自己的权限** —— 他不需要"攻破"本项目。
  这属于主机安全范畴，不是本项目的漏洞。
- **同机恶意软件**同理。它能读你的家目录，自然也能读模型、缓存和证书。
- 本项目**不提供**沙箱、不提供进程隔离、也不打算提供 —— 那不是单机工具该背的东西。

### 默认姿态（都是硬约束，见 `docs/SPEC.md`）

| 约束 | 行为 |
|---|---|
| 绑定地址 | 默认 **只绑 `127.0.0.1`** |
| 非回环绑定（`0.0.0.0` / 局域网 IP） | **强制启用 token**，不指定就随机生成并打印一次。逗号分隔绑多个地址时，**任一个**非回环即按非回环处理 |
| 浏览器跨站请求（`Sec-Fetch-Site: cross-site`） | **直接 403**。这个头由浏览器强制写、JS 改不了，是 DNS rebinding 在 HTTP 层唯一不可伪造的指纹；`curl` / 脚本不带它，不受影响 |
| 逃生舱 `--allow-no-auth` | 非回环下允许关鉴权、放行明文与弱 token（三条硬拦降级为警告）—— **测试 / 可信局域网专用**，同一网段任何人都能直接用你的 NPU |
| 非回环 + 明文 | **默认拒绝启动**（退出码 3），必须显式加 `--allow-insecure` 才放行，且放行后**仍然强制 token** |
| TLS | `nputweb` 默认自签 HTTPS；自签证书与私钥落在 `~/.nputweb/`（**不入库**） |
| 错误响应 | 只回 `{"error": {"code": ..., "message": ...}}`，**绝不回 traceback**（traceback 里带本机绝对路径） |
| 文档端点 | `/docs` `/redoc` 默认 404，`--debug` 才开 |
| 绑定具体地址 | 只是**收窄暴露面**，不是访问控制：Host 头可以任意伪造，能当门的只有 token |

顺带澄清一条常被当成漏洞的事：**自签证书的浏览器警告是设计如此**，
不是"证书坏了"。项目是离线的，没有任何 CA 能给你签一张本机回环地址的证书。
首次启动时终端会打印 **SHA-256 指纹**，请与浏览器里显示的核对一次 —— 这是防中间人的，不是可跳过的步骤。

### 数据出不出本机

- 运行时**零外链**：无遥测、无 CDN、无在线词表。
- 模型权重在本地 `models/`（被 `.gitignore` 排除），译文缓存在进程内，NPUW 编译缓存在 `.npucache/`。
- 唯一的"外链"是你**主动**跑 `scripts/fetch_model.py` 下载模型 —— 那时走的是 ModelScope / HF，你自己选的源。

---

## 明确**不属于**漏洞的情形

以下情形我们会关掉，**不是因为不在乎，而是因为按上面的威胁模型，它们不在本项目的防护范围内**。
把下面这些当漏洞报上来，只会挤掉真正的安全问题：

1. **把服务绑到 `0.0.0.0`、或显式关掉鉴权（`--no-auth` / `NPT_SERVE_NO_AUTH`）之后，被同一网络里的人访问 / 扫描到。**
   这是你自己改了默认姿态。非回环绑定会强制 token，你又手动关掉了它 —— 或者用 `--allow-no-auth`
   主动解除了那几条硬拦 —— 剩下的暴露面是你选的。
2. **能登录本机、能物理接触本机、或已在同机运行恶意软件**的攻击者读到了模型 / 缓存 / 证书 / 译文。
   他用的是你自己的权限。
3. **`models/`、`.npucache/`、日志、译文缓存没有加密**。它们是本机文件，保护来自操作系统的文件权限，不是本项目。
4. **自签证书的浏览器警告**；以及"我把证书指纹抄错了" —— 首次启动请核对终端里打印的 SHA-256 指纹。
5. **DoS / 资源耗尽**：限流（`--rate`）、队列（`--queue`）、单次输入上限（`--max-input-chars`）是为了**防止你自己踩自己**
   （比如前端心跳把配额吃光），不是抗分布式攻击的能力。
6. **上游组件自身的漏洞**（OpenVINO、FastAPI / Starlette、uvicorn、cryptography、模型权重文件本身）。
   应当向各自的上游报告；我们跟随升级。
   —— 但**如果你发现我们锁定的版本组合里含有已知 CVE**，请报给我们，那条算。
7. **翻译质量 / 语种支持 / 性能数字**问题：那是 bug 或能力限制，走普通 issue，不是安全问题。
8. **"我在公开 issue 里贴了私钥"**：请立刻吊销并删除那份证书（`~/.nputweb/` 下的对应文件）、重新生成；
   这不是本项目的漏洞，但**是**一次真实泄漏 —— 处理它比报给我们更紧急。

---

## 如果你只是想确认"我这样用安全吗"

三条就够：

1. **别改默认绑定地址。** 需要局域网访问时，保留 token，保留 TLS；别加 `--allow-insecure` / `--allow-no-auth` 图省事。
2. **token 只出现一次。** 它启动时打印在终端、可能带在打开的 URL 里；别把它贴进日志、截图、issue。
3. **私钥不入库、不外发。** `~/.nputweb/` 与 `models/`、`.npucache/` 一样，都应该是"只在这台机器上"的东西。

---

## 相关文档

- 技术契约（安全基线的权威定义）：[`docs/SPEC.md`](docs/SPEC.md)
- 上手与常见问题：[`README.md`](README.md)
- 贡献与代码约定：[`CONTRIBUTING.md`](CONTRIBUTING.md)
